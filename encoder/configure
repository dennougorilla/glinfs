#!/bin/sh
(cd vendor/libimagequant && ./configure --disable-sse CC=emcc)
(cd vendor/gifsicle && autoreconf -i && ./configure --host=wasm32 --disable-gifview --disable-gifdiff --disable-simd --disable-threads CC=emcc)